"use client";
import Link from "next/link"; import { useParams,useRouter } from "next/navigation"; import { useCallback,useEffect,useState } from "react"; import { AuthGuard } from "@/components/auth/AuthGuard"; import { useAuth } from "@/components/auth/AuthProvider"; import { TradeForm } from "@/components/trades/TradeForm"; import { getTrade,updateTrade } from "@/lib/firebase/firestore"; import type { TradeDraft } from "@/types/trade";
export default function EditTrade(){return <AuthGuard><Edit/></AuthGuard>}
function Edit(){const {user}=useAuth(),{id}=useParams<{id:string}>(),router=useRouter(),[draft,setDraft]=useState<TradeDraft|null>(null),[loading,setLoading]=useState(true),[error,setError]=useState(""),[busy,setBusy]=useState(false);const load=useCallback(async()=>{if(!user)return;try{const t=await getTrade(user.uid,id);if(!t){setError("This trade was not found or is no longer available.");return}const {id:_id,createdAt:_created,updatedAt:_updated,...editable}=t;void _id;void _created;void _updated;setDraft(editable)}catch(err){console.error(err);setError("This trade could not be loaded. Please try again.")}finally{setLoading(false)}},[user,id]);useEffect(()=>{void load()},[load]);if(loading)return <main className="center-state"><span className="loader"/>Loading trade…</main>;if(error||!draft)return <main className="app-page"><div className="empty-state"><div className="empty-icon">?</div><h2>Unable to edit trade</h2><p>{error||"This trade is unavailable."}</p><Link className="button" href="/trades">Back to journal</Link></div></main>;return <main className="app-page"><div className="page-heading"><div><p className="eyebrow"><i/>Journal</p><h1>Edit {draft.symbol}</h1><p className="page-subtitle">Update the fields below and save your changes.</p></div></div><TradeForm value={draft} onChange={setDraft} busy={busy} submitLabel="Update trade" onCancel={()=>router.push(`/trades/${id}`)} onSubmit={async value=>{if(!user)return;setBusy(true);try{await updateTrade(user.uid,id,value);router.push(`/trades/${id}`)}finally{setBusy(false)}}}/></main>}
