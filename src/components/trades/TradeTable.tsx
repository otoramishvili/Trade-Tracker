import Link from "next/link"; import type { Trade } from "@/types/trade";
export function TradeTable({trades,onDelete}:{trades:Trade[];onDelete:(t:Trade)=>void}){return <div className="table-wrap"><table><thead><tr>{["Date","Symbol","Side","Session","R:R","P&L","Outcome","Actions"].map(x=><th key={x}>{x}</th>)}</tr></thead><tbody>{trades.map(t=><tr key={t.id}><td>{t.date}</td><td><b>{t.symbol}</b></td><td>{t.position}</td><td>{t.session?.replace("_"," ")??"—"}</td><td>{t.rr??"—"}</td><td className={(t.pnl??0)>0?"positive":(t.pnl??0)<0?"negative":""}>{t.pnl??"—"}</td><td><span className={`pill ${t.outcome??""}`}>{t.outcome??"—"}</span></td><td className="row-actions"><Link href={`/trades/${t.id}`}>View</Link><Link href={`/trades/${t.id}/edit`}>Edit</Link><button onClick={()=>onDelete(t)}>Delete</button></td></tr>)}</tbody></table></div>}
