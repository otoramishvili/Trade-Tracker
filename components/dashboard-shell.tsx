"use client";
import Link from "next/link";import {usePathname,useRouter} from "next/navigation";import {useEffect,useState} from "react";import {useAuth} from "@/lib/auth-context";import {BarChart3,CalendarDays,ChevronLeft,ChevronRight,LayoutDashboard,LogOut,Menu,Plus,WalletCards,X} from "lucide-react";
const links=[{href:"/dashboard",label:"Overview",icon:LayoutDashboard},{href:"/dashboard/trades",label:"Trades",icon:BarChart3},{href:"/dashboard/calendar",label:"Calendar",icon:CalendarDays},{href:"/dashboard/accounts",label:"Accounts",icon:WalletCards}];
export function DashboardShell({children}:{children:React.ReactNode}){const path=usePathname();const router=useRouter();const {user,loading,logout}=useAuth();const [open,setOpen]=useState(false);useEffect(()=>{if(!loading&&!user)router.replace("/login")},[loading,user,router]);if(loading||!user)return <div className="loading"><span/></div>;const email=user.email||"trader@edgelog.app";return <div className="app-shell"><aside className={open?"open":""}><div className="aside-head"><Link href="/" className="brand"><span>EL</span> EdgeLog</Link><button onClick={()=>setOpen(false)}><X/></button></div><nav>{links.map(({href,label,icon:Icon})=><Link key={href} href={href} onClick={()=>setOpen(false)} className={path===href?"active":""}><Icon/>{label}</Link>)}</nav><div className="account-chip"><div>{email.slice(0,2).toUpperCase()}</div><span><strong>{email.split("@")[0]}</strong><small>{email}</small></span><button onClick={()=>logout()} title="Log out"><LogOut/></button></div></aside><main className="workspace"><header><button className="mobile-menu" onClick={()=>setOpen(true)}><Menu/></button><div><p>TRADING JOURNAL</p><h1>{links.find(l=>l.href===path)?.label||"Overview"}</h1></div><Link href="/dashboard/trades?new=1" className="button small"><Plus/> Log trade</Link></header>{children}</main></div>}
export function PageTitle({eyebrow,title,description,action}:{eyebrow:string;title:string;description:string;action?:React.ReactNode}){return <div className="page-title"><div><small>{eyebrow}</small><h2>{title}</h2><p>{description}</p></div>{action}</div>}
export function EmptyState({icon:Icon,title,text,action}:{icon:typeof BarChart3;title:string;text:string;action?:React.ReactNode}){return <div className="empty"><Icon/><h3>{title}</h3><p>{text}</p>{action}</div>}
